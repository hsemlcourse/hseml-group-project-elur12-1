# Core
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# ML — gradient boosting
xgboost>=2.0.0
lightgbm>=4.0.0

# ML — deep learning (PyTorch с поддержкой MPS на macOS)
torch>=2.1.0

# Visualization & dim reduction
matplotlib>=3.7.0
seaborn>=0.12.0
umap-learn>=0.5.4

# Code quality (do not remove — used by CI)
ruff>=0.1.0
flake8>=6.1.0
pytest>=7.4.0
papermill>=2.4.0

# Notebooks (для papermill в CI)
ipykernel>=6.0.0
jupyter>=1.0.0

# Utilities
joblib>=1.3.0
tqdm>=4.65.0
python-dotenv>=1.0.0
pillow>=10.0.0

# Deploy (FastAPI + ASGI сервер)
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.5.0
